import { ViewStyle } from "react-native";

// Pre-computed rotation styles to avoid re-creation
export const ROTATE_45: ViewStyle = { transform: [{ rotate: "45deg" }] };

export const ROTATE_NEG_45: ViewStyle = { transform: [{ rotate: "-45deg" }] };
